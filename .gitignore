# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor/IDE files
*.swp
*.swo
*~
.idea/
.vscode/
*.sublime-project
*.sublime-workspace

# Temporary files
*.tmp
*.temp
*.log
*.bak

# Build artifacts
*.o
*.pyc
__pycache__/

# Node modules (if any scripts use them)
node_modules/

# Chezmoi runtime files (these are created in ~/.config/chezmoi/, not here)
# But in case they get created locally during testing:
.chezmoi.toml
chezmoi.toml

# Package manager locks that might be created during testing
package-lock.json
yarn.lock
pnpm-lock.yaml

# Local testing/development
.env
.env.local
*.local
# ...except chezmoi sources for ~/*.local targets. These are `create_` files:
# chezmoi writes them once on a fresh machine, then never again. Without this
# negation they stay untracked and silently never reach a new install.
!home/create_dot_*.local

# Backup files from editors
*~
\#*\#
