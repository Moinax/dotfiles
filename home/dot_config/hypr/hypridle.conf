general {
    # --no-fade-in is not cosmetic here, it is what lets the settle in
    # lock-before-sleep.sh be short: hyprlock's fade blends *from a screencopy of
    # the desktop*, so its first commits still show what the lock is hiding, and
    # anything waiting for "the frame landed" has to outlast the whole animation
    # rather than the lock. --immediate-render drops the resource gather before
    # the first draw (~143ms measured) for the same reason.
    lock_cmd = pidof hyprlock || hyprlock --no-fade-in --immediate-render
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch 'hl.dsp.dpms({ action = "enable" })'
    inhibit_sleep = 3
}


listener {
    timeout = 250
    on-timeout = notify-send "Your computer will be locked due to inactivity"
}

listener {
    timeout = 300
    on-timeout = loginctl lock-session
}

listener {
    timeout = 330
    on-timeout = hyprctl dispatch 'hl.dsp.dpms({ action = "disable" })'
    on-resume = hyprctl dispatch 'hl.dsp.dpms({ action = "enable" })'
}

listener {
    timeout = 1800
    on-timeout = systemctl suspend
}
