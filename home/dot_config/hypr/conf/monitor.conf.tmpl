{{- if eq .chezmoi.hostname "moinax-desktop" }}
### LAYOUT: three_monitors ###
# This file defines the three-monitor setup.

# Left Monitor (DP-2) - Vertical
monitor=DP-2,2560x1440,-1440x-500,1,transform,1

# Center Monitor (DP-3) - Horizontal.
# Defaults to SDR (8-bit, srgb); Mod+Alt+H toggles 10-bit HDR at runtime (toggle-hdr.sh).
monitor=DP-3,3440x1440@174.96,0x0,1

# Right Monitor (DP-1) - Vertical
monitor=DP-1,2560x1440,3440x-500,1,transform,1
{{- else if eq .chezmoi.hostname "moinax-laptop" }}
# Internal laptop panel (eDP-1): 3840x2400 4K.
# Hyprland rejects scale=1.75 ("Invalid scale passed to monitor, 1.75 found
# suggestion 1.6666666") because 3840/1.75 isn't an integer number of logical
# pixels. Use Hyprland's own suggestion, 1.6666666 → 3840/1.6666666 = 2304
# logical width, which applies cleanly with no reload warning.
monitor=eDP-1,preferred,auto,1.6666666
# Fallback for any external display plugged into the laptop.
monitor=,preferred,auto,1
{{- else }}
monitor=,preferred,auto,1
{{- end }}
